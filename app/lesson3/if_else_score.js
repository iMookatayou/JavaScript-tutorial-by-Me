// ให้คะแนนไว้ที่ 50
let score = 50;
// ถ้าคะแนนมากกว่าหรือเท่ากับ 60 ให้แสดงผล "ผ่าน" ถ้าน้อยกว่า 60 ให้แสดงผล "ไม่ผ่าน"
let isPass = score >= 60;

// ถ้า isPass เป็นจริง ให้แสดง "ผ่าน" ถ้าไม่ใช่ ให้แสดง "ไม่ผ่าน"
if (isPass) {
    console.log("ผ่าน");
} else { 
    console.log("ไม่ผ่าน");
}
